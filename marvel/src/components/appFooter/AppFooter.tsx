import React, { FC } from 'react';

import Toolbar from '@mui/material/Toolbar';

import './appFooter.css';

const AppFooter: FC = () => {
  return (
    <footer className="footer">
      <Toolbar>fsdfsd</Toolbar>
    </footer>
  );
};

export default AppFooter;
